fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2
# Anlamsal eşleştirme (üretim) — tamamen lokal, internetsiz çalışır:
sentence-transformers>=2.6
# İsteğe bağlı vektör veritabanı (ölçeklenince):
# chromadb>=0.5
