# Canlı (Render / HF Spaces) bağımlılıkları — API embedding yolu.
# Embedding modeli SUNUCUDA TUTULMAZ; sorgular Gemini API'ye gider (embed_api.py,
# yalnızca stdlib). Bu yüzden fastembed/onnxruntime/torch YOK -> uygulama ~80 MB,
# Render Free (512 MB) rahat sığar, soğuk başlangıç anında.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2
